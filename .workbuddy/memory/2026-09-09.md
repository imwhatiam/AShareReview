# 2026-09-09

> **状态标注（2026-09-12 补注，未改动下文原记录）**：本文记的是当日状态，以下几条后来已变，**不要照本文执行**：
> - 「开盘啦与东方财富页合并为共享视图」：东方财富模块已于 2026-09-11 彻底删除，`SectorFlowView` 现在只服务开盘啦。
> - 「`src/shared/ui/` 新增 DateField、共用 `DateField`」：`DateField.jsx` 已被 `DatePicker.jsx` 取代并删除；`shared/ui/` 现为 `DatePicker` / `Icon` / `Panel` / `SegmentedControl` / `StatRow` / `TabBar`。
> - 「视觉验证方法：临时建 `frontend/preview.html` + `preview/main.jsx` 走 vite 截图」：已被 `.workbuddy/skills/frontend-visual-verification/SKILL.md` 的三条通道取代；且一切快照/探针产物必须落在 `tests/`、`frontend/tests/`，**不得写仓库根**（根目录 3 个 `*-preview.html` 已于 2026-09-12 取消 git 跟踪并加入 `.gitignore`）。

## 前端 UI 重设计（不改功能）

- 对 `frontend/` 完成整体 UI 重设计，功能、API 路径、交互契约全部保持不变；50 个既有前端测试全部通过，`npm run lint`、`npm run build` 通过。
- 设计令牌：`src/styles/tokens.css` 全面扩充（品牌蓝 + 中性色 + 红涨绿跌语义色 + 间距/圆角/阴影/字体刻度）。原则：界面外壳用蓝色系，红/绿只用于数据语义。
- 全局样式重写：`src/index.css`（外壳布局、粘性玻璃拟态头部、面板、选项卡、分段控件、表格、排行、状态、登录页、响应式）。注意：`index.css` 必须字面包含 `:focus-visible`、`@media (max-width: 40rem)`、`overflow-x: auto`（`DataState.test.jsx` 直接读文件断言）；`tokens.css` 必须保留 `--color-market-up/down/error` 且取值互不相同。
- 新增共享组件层 `src/shared/ui/`：TabBar（漫游 tabindex + 方向键，一级/二级导航共用）、Panel、DateField、SegmentedControl、StatRow、Icon。
- 新增图表基线 `src/shared/charts/`：chartTheme.js（纯数据，不 import echarts，因测试 mock 只有 `init`）+ useChart.js（init/setOption(notMerge)/resize/dispose 生命周期）。四个 ECharts 组件统一接(theme)。测试断言 `option.xAxis.data`、`option.series[0].data` 原样传递，不能改动数据形状。
- 开盘啦与东方财富页合并为共享视图 `src/features/sector-flow/SectorFlowView.jsx`，两个页面只剩文案/钩子/错误提示差异。
- 关键约束：页面里很多断言用 getByText 精确匹配（如 `新高：2 只（20.00%）`），依赖 testing-library 的 getNodeText 只拼接“元素直接文本子节点”，因此这些整句必须保持在同一个元素内、不能被 `<strong>` 等子元素拆开；新增包裹元素时其直接文本必须为空或与断言无关。
- 视觉验证方法（可复用）：临时建 `frontend/preview.html` + `preview/main.jsx`（给 `<App fetchImpl={mockFetch} />` 传 mock 数据），`npx vite` 起本地服务，用 headless Chrome `--screenshot` 逐页截图，验证后删除。vite 默认只监听 IPv6 ::1，curl 要用 `http://localhost`。
- `frontend/dist/` 已用新代码重建（后端直接服务 dist）。
